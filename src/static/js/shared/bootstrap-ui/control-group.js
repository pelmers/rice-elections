// Generated by CoffeeScript 1.6.2
define([], function() {
  var ControlGroup;

  return ControlGroup = (function() {
    function ControlGroup(el) {
      var _this = this;

      this.el = el != null ? el : null;
      ({
        /*
            Set error.
            Args:
                msg {String}: error message string.
        */

        setError: function(msg) {
          var error;

          _this.removeError();
          if (_this.el) {
            _this.el.addClass('error');
            error = $('<div>', {
              "class": 'controls help-inline error-msg'
            }).append(msg);
            return _this.el.append(error);
          } else {
            return alert("Error: " + msg);
          }
        },
        /*
            Remove error.
        */

        removeError: function() {
          if (_this.el) {
            _this.el.removeClass('error');
            return _this.el.children().filter('.error-msg').remove();
          }
        }
      });
    }

    return ControlGroup;

  })();
});
