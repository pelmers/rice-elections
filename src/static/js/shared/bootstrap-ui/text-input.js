// Generated by CoffeeScript 1.6.2
var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

define(['bootstrap-ui/control-group'], function(ControlGroup) {
  var TextInput;

  return TextInput = (function() {
    function TextInput(el, options) {
      this.el = el;
      if (options == null) {
        options = {};
      }
      this.setValue = __bind(this.setValue, this);
      this.getValue = __bind(this.getValue, this);
      this.required = options.required || false;
      this.controlGroup = new ControlGroup(options.controlGroup);
    }

    TextInput.prototype.getValue = function() {
      var inputString;

      inputString = this.el.val().trim();
      if (this.required && !inputString) {
        this.controlGroup.setError('Required field.');
        return null;
      }
      this.removeError();
      return inputString;
    };

    TextInput.prototype.setValue = function(val) {
      this.el.val(val);
      return this.controlGroup.removeError();
    };

    return TextInput;

  })();
});
